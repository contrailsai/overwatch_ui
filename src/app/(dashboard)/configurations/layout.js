export const metadata = {
  title: 'overwatch - Configurations',
  description: 'Manage your account and notification settings.',
};

export default function ConfigurationsLayout({ children }) {
  return children;
}
